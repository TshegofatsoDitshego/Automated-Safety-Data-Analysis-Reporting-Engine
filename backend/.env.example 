# Application Settings
APP_NAME=SafetySync Analytics Platform
ENVIRONMENT=development
LOG_LEVEL=INFO

# Database Configuration (TimescaleDB/PostgreSQL)
DATABASE_URL=postgresql://safety_admin:safety_dev_2024@timescaledb:5432/safety_analytics

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Celery Configuration
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Data Pipeline Settings
MAX_BATCH_SIZE=1000
INGESTION_BUFFER_SIZE=5000
DATA_RETENTION_DAYS=90

# Data Quality Thresholds
MAX_DUPLICATE_RATE=0.05
MAX_INVALID_RATE=0.10
MAX_LATE_ARRIVAL_MINUTES=60

# Alert Thresholds (adjust per your requirements)
GAS_DETECTION_THRESHOLD=10.0
TEMPERATURE_THRESHOLD=60.0
PRESSURE_THRESHOLD=150.0

# Reporting
REPORTS_OUTPUT_DIR=/app/reports
REPORT_RETENTION_DAYS=365

# API Settings
API_V1_PREFIX=/api/v1
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000"]